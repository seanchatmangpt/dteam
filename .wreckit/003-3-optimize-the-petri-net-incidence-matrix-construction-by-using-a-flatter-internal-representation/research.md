I will begin by searching for "incidence matrix" and related terms in the codebase to identify the current implementation and its usage.
I will read `src/models/petri_net.rs` to examine the `incidence_matrix` function and the `PetriNet` struct.
The current `incidence_matrix` implementation returns a heap-heavy `Vec<Vec<i32>>` and uses a `PackedKeyTable` for construction each time. A flatter representation, such as a single `Vec<i32>`, would improve CPU cache locality and GPU transfer, aligning with the "Vision 2030 Roadmap." I will now investigate `src/conformance/case_centric/token_based_replay.rs` and `src/conformance/mod.rs` to see how transition firing is currently implemented and identify where this optimization can be applied.
I will now read `src/conformance/mod.rs` and search for "replay" or "fire" to understand the current implementation.
In `src/conformance/mod.rs`, `input_masks` and `output_masks` are built on the fly, and the firing rule uses these masks in a branchless execution kernel. The directive to optimize incidence matrix construction likely suggests storing a flatter representation, such as a `Vec<i32>` or bitset masks, directly in the `PetriNet` struct to avoid repetitive indexing. Since `PetriNet` currently relies on `Vec<Place>`, `Vec<Transition>`, and `Vec<Arc>`, I will now examine `src/utils/dense_kernel.rs` to identify better-suited data structures for this optimization.
`DenseIndex` and `KBitSet` appear to be key components for a flatter internal representation. I suspect `DenseIndex` could map places and transitions to `DenseId`s, while a flat array would store incidence values. I'll now check `src/dteam/mod.rs` and potentially `src/dteam/core.rs` for the `KTier` definition, as the project overview indicates its importance for markings and transition masks.
I misunderstood the directory structure. I'll check `src/lib.rs` to find where the core logic is defined.
The `dteam` modules, including `KTier` and `Engine`, are defined in `src/lib.rs`, while `PackedKeyTable` resides in `src/utils/dense_kernel.rs`. The current `incidence_matrix` returns a `Vec<Vec<i32>>`, which is inefficient; a flatter `Vec<i32>` or a structure using `KBitSet` for masks would be more performant, especially for the "Vision 2030 Roadmap" goals. I'll now check if `incidence_matrix` is used beyond `src/models/petri_net.rs`, `VISION_2030_ROADMAP.md`, and `plans/powl-to-petrinet-review.md`.
`incidence_matrix` is currently only in `src/models/petri_net.rs`. To achieve "nanosecond-scale updates," I'm considering refactoring `PetriNet` to include pre-compiled structures like `DenseIndex` and flat incidence matrices, rather than rebuilding lists. I'll also check `Cargo.toml` for `bcinr`, as it's a mentioned dependency for bitset primitives.
`zero_allocation_bench.rs` and `bcinr_primitives_bench.rs` should clarify the project's performance goals. I'll examine `benches/zero_allocation_bench.rs` for their zero-allocation patterns.
`dteam::ref_models::ref_petri_net::PetriNet` caught my attention; I'd previously seen `PetriNet` in `src/models/petri_net.rs`. I need to investigate what `ref_models` is.
I'll read `src/ref_models/ref_petri_net.rs`.
`ref_models` appears to be a reference implementation, likely less optimized than `src/models/`. I'll now examine `src/ref_conformance/ref_token_replay.rs`.
I've identified three replayer versions, with the incidence matrix implicitly represented in the optimized and BCINR versions. The directive to optimize incidence matrix construction likely targets the `Vec<Vec<i32>>` return type in `src/models/petri_net.rs`, which is inefficient due to multiple heap allocations and poor cache locality. My research report will detail these inefficiencies, propose a flat `Vec<i32>` representation, and explore integration points like `verifies_state_equation_calculus` and future GPU offloading. I'll also investigate any existing "FlatMatrix" implementations in the codebase.
`src/powl/core.rs` uses a flattened bitmask representation for high-speed replay, a pattern the directive wants to apply to Petri Nets. Currently, `PetriNet` in `src/models/petri_net.rs` is not flattened, and its `incidence_matrix` function is inefficient. I'll check if `src/powl/conversion/to_petri_net.rs` has been created since the last review, as it might contain a more optimized implementation.
It exists! I'm checking `src/powl/conversion/` now.
I'll read `src/powl/conversion/to_petri_net.rs`.
POWL-to-`PetriNet` conversion is standard, but `incidence_matrix` and `build_node_index` in `src/models/petri_net.rs` are slow for large nets due to repetitive index building and inefficient matrix representation. I'll propose avoiding index rebuilding, using a flatter matrix, and potentially caching it in `PetriNet`. This optimization is crucial because `token_replay_projected` currently fails for nets with over 64 places. I'll also check `src/models/mod.rs` for other relevant information.
The current incidence matrix implementation in `src/models/petri_net.rs` is inefficient due to its jagged `Vec<Vec<i32>>` structure, repeated index rebuilding, and poor cache locality, making it a bottleneck for large nets. Aligning with Vision 2030's GPU offloading goals and the existing "flattened bitmask" pattern in `PowlModel`, I propose replacing it with a `FlatIncidenceMatrix` struct using `DenseIndex` for pre-calculation and a single `Vec<i32>` for storage within `PetriNet`. This will optimize several integration points, including `verifies_state_equation_calculus` and `token_replay_projected`. I'm now drafting the `research.md` report.
I will now write the research report to `plans/research.md` detailing the findings and proposed optimizations for the Petri Net incidence matrix construction.
I have completed the research and written a detailed report analyzing the current incidence matrix implementation, highlighting inefficiencies, and proposing a strategy to use a flatter internal representation aligned with the codebase's existing zero-allocation patterns. I will now exit plan mode to finalize the research phase.
I have completed the research on optimizing the Petri Net incidence matrix construction. My analysis identified significant inefficiencies in the current jagged `Vec<Vec<i32>>` representation, which involves redundant heap allocations and poor cache locality. I have proposed a flatter 1D array representation using the existing `DenseIndex` primitive to align with the project's zero-allocation mandate and the Vision 2030 Roadmap for GPU acceleration.

A detailed research report has been generated at `plans/research.md`.

### Key Findings:
- **Current Bottleneck:** `PetriNet::incidence_matrix()` rebuilds its index on every call and returns a heap-heavy jagged array.
- **Architectural Alignment:** The proposed flat representation follows established patterns seen in `PowlModel` and `ProjectedLog`.
- **Primary Integration Points:** Structural workflow-net validation, state equation calculus, and future WebGPU compute shaders.
- **Proposed Solution:** Introduce a `FlatIncidenceMatrix` struct and a `compile()` method in `PetriNet` to cache the contiguous 1D matrix and `DenseIndex`.
