# Commands

```bash
make audit
make doctor
make acceptance
make doctor-artifacts
```

`make audit` and `make acceptance` do not require the Rust dependency closure. `make doctor` adds environment and sibling-workspace observations. `make doctor-artifacts` retains the existing AutoML-plan pathology diagnosis.
