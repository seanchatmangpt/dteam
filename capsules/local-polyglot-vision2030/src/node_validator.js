const fs=require('fs'),path=require('path'),crypto=require('crypto');const root=path.resolve(__dirname,'..');
const checkpoints=fs.readFileSync(path.join(root,'innovation-checkpoints.tsv'),'utf8').trim().split(/\n/).slice(1);const caps=fs.readFileSync(path.join(root,'capabilities.tsv'),'utf8').trim().split(/\n/).slice(1);const axes=fs.readFileSync(path.join(root,'axes.tsv'),'utf8').trim().split(/\n/).map(x=>x.split('\t')[1].split(','));const product=axes.reduce((a,v)=>a*v.length,1);if(caps.length!==24||checkpoints.length!==10||product!==8640)process.exit(2);const sha256=crypto.createHash('sha256').update(fs.readFileSync(path.join(root,'canonical.bin'))).digest('hex');console.log(JSON.stringify({language:'node',capabilities:24,checkpoints:10,profiles:8640,sha256,standing:'ALIVE'}));
